apiVersion: v1
kind: Pod

metadata:
name: web-pod

spec:
containers:

* name: nginx
  image: nginx

  volumeMounts:

  * mountPath: /data
    name: data-volume

volumes:

* name: data-volume
  emptyDir: {}
